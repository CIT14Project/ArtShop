var loggedIn = false;
    function bought() {
        if(loggedIn==false) {
            alert("You need to log in first.");
        }
    }
    function addedToCart() {
            alert("Added to Cart.");
    }